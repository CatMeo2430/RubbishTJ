<Application x:Class="Taiji.GUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <!-- One Dark https://github.com/atom/one-dark-syntax -->
    <Color x:Key="OdBg">#FF282C34</Color>
    <Color x:Key="OdBgDeep">#FF21252B</Color>
    <Color x:Key="OdBgSoft">#FF2C313A</Color>
    <Color x:Key="OdGutter">#FF3E4451</Color>
    <Color x:Key="OdFg">#FFFFFFFF</Color>
    <Color x:Key="OdFgDim">#FFFFFFFF</Color>
    <Color x:Key="OdCyan">#FF56B6C2</Color>
    <Color x:Key="OdBlue">#FF61AFEF</Color>
    <Color x:Key="OdPurple">#FFC678DD</Color>
    <Color x:Key="OdGreen">#FF98C379</Color>
    <Color x:Key="OdRed">#FFE06C75</Color>
    <Color x:Key="OdOrange">#FFD19A66</Color>
    <Color x:Key="OdYellow">#FFE5C07B</Color>

    <SolidColorBrush x:Key="BrushBg" Color="{StaticResource OdBg}" />
    <SolidColorBrush x:Key="BrushBgDeep" Color="{StaticResource OdBgDeep}" />
    <SolidColorBrush x:Key="BrushBgSoft" Color="{StaticResource OdBgSoft}" />
    <SolidColorBrush x:Key="BrushGutter" Color="{StaticResource OdGutter}" />
    <SolidColorBrush x:Key="BrushFg" Color="{StaticResource OdFg}" />
    <SolidColorBrush x:Key="BrushFgDim" Color="{StaticResource OdFgDim}" />
    <SolidColorBrush x:Key="BrushWhite" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="BrushCyan" Color="{StaticResource OdCyan}" />
    <SolidColorBrush x:Key="BrushBlue" Color="{StaticResource OdBlue}" />
    <SolidColorBrush x:Key="BrushPurple" Color="{StaticResource OdPurple}" />
    <SolidColorBrush x:Key="BrushGreen" Color="{StaticResource OdGreen}" />
    <SolidColorBrush x:Key="BrushRed" Color="{StaticResource OdRed}" />
    <SolidColorBrush x:Key="BrushOrange" Color="{StaticResource OdOrange}" />
    <SolidColorBrush x:Key="BrushYellow" Color="{StaticResource OdYellow}" />
    <SolidColorBrush x:Key="BrushPanel" Color="#F221252B" />
    <SolidColorBrush x:Key="BrushDivider" Color="#FF3E4451" />

    <FontFamily x:Key="UiFont">Cascadia Code, Consolas, Microsoft YaHei UI, Segoe UI</FontFamily>

    <Style TargetType="Window">
      <Setter Property="Background" Value="{StaticResource BrushBg}" />
      <Setter Property="Foreground" Value="{StaticResource BrushFg}" />
      <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
      <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource BrushFg}" />
    </Style>

    <Style TargetType="ToolTip">
      <Setter Property="Background" Value="{StaticResource BrushBgSoft}" />
      <Setter Property="Foreground" Value="{StaticResource BrushFg}" />
      <Setter Property="BorderBrush" Value="{StaticResource BrushGutter}" />
    </Style>

    <Style TargetType="ContextMenu">
      <Setter Property="Background" Value="{StaticResource BrushBgSoft}" />
      <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
      <Setter Property="BorderBrush" Value="{StaticResource BrushGutter}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="2" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ContextMenu">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Padding="{TemplateBinding Padding}"
                    CornerRadius="4">
              <StackPanel IsItemsHost="True"
                          KeyboardNavigation.DirectionalNavigation="Cycle" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="MenuItem">
      <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
      <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style x:Key="OdContextMenuItem" TargetType="MenuItem">
      <Setter Property="OverridesDefaultStyle" Value="True" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Icon" Value="{x:Null}" />
      <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="14,8" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="MenuItem">
            <Border x:Name="Bd"
                    Background="Transparent"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="True">
              <ContentPresenter ContentSource="Header"
                                RecognizesAccessKey="True"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                TextElement.Foreground="{StaticResource BrushWhite}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushGutter}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Button -->
    <Style x:Key="OdButton" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource BrushGutter}" />
      <Setter Property="Foreground" Value="{StaticResource BrushFg}" />
      <Setter Property="BorderBrush" Value="{StaticResource BrushGutter}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="12,6" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#FF4B5263" />
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushBlue}" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#FF3E4451" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource OdButton}" />

    <Style x:Key="OdButtonPrimary" TargetType="Button" BasedOn="{StaticResource OdButton}">
      <Setter Property="Background" Value="#FF3A4A5C" />
      <Setter Property="BorderBrush" Value="{StaticResource BrushBlue}" />
      <Setter Property="Foreground" Value="{StaticResource BrushBlue}" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="OdButtonFlat" TargetType="Button" BasedOn="{StaticResource OdButton}">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="OdButtonDanger" TargetType="Button" BasedOn="{StaticResource OdButtonFlat}">
      <Setter Property="Foreground" Value="{StaticResource BrushRed}" />
    </Style>

    <!-- TextBox -->
    <Style x:Key="OdTextBox" TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BrushBgDeep}" />
      <Setter Property="Foreground" Value="{StaticResource BrushFg}" />
      <Setter Property="CaretBrush" Value="{StaticResource BrushBlue}" />
      <Setter Property="BorderBrush" Value="{StaticResource BrushGutter}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="10,8" />
      <Setter Property="SelectionBrush" Value="#604B5263" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}">
              <ScrollViewer x:Name="PART_ContentHost" Background="Transparent" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushBlue}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource OdTextBox}" />

    <!-- ComboBox -->
    <ControlTemplate x:Key="OdComboToggle" TargetType="ToggleButton">
      <Border x:Name="Bd" Background="{StaticResource BrushBgSoft}"
              BorderBrush="{StaticResource BrushGutter}" BorderThickness="1" CornerRadius="4">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="28" />
          </Grid.ColumnDefinitions>
          <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                Data="M 0,0 L 5,5 L 10,0" Stroke="{StaticResource BrushWhite}"
                StrokeThickness="1.5" Fill="Transparent" />
        </Grid>
      </Border>
      <ControlTemplate.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushBlue}" />
        </Trigger>
        <Trigger Property="IsChecked" Value="True">
          <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushCyan}" />
        </Trigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="ComboBoxItem">
      <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="8,4" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                TextElement.Foreground="{StaticResource BrushWhite}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrushGutter}" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#FF3A4A5C" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="OdCombo" TargetType="ComboBox">
      <Setter Property="Background" Value="{StaticResource BrushBgSoft}" />
      <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
      <Setter Property="BorderBrush" Value="{StaticResource BrushGutter}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="8,4" />
      <Setter Property="Height" Value="34" />
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
      <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <ToggleButton x:Name="ToggleButton" Template="{StaticResource OdComboToggle}"
                            Focusable="False" ClickMode="Press"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
              <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                Margin="10,0,32,0" VerticalAlignment="Center" HorizontalAlignment="Left"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding ItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                TextElement.Foreground="{StaticResource BrushWhite}" />
              <Popup x:Name="Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False"
                     IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Fade">
                <Border MinWidth="{TemplateBinding ActualWidth}" MaxHeight="320"
                        Background="{StaticResource BrushBgDeep}"
                        BorderBrush="{StaticResource BrushBlue}" BorderThickness="1"
                        CornerRadius="4" Padding="2" Margin="0,4,0,0"
                        TextElement.Foreground="{StaticResource BrushWhite}">
                  <ScrollViewer>
                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ComboBox" BasedOn="{StaticResource OdCombo}" />

    <!-- ListBox -->
    <Style TargetType="ListBox">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Foreground" Value="{StaticResource BrushWhite}" />
    </Style>

    <Style x:Key="OdListItem" TargetType="ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0,0,0,6" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="Bd" Background="{StaticResource BrushBgSoft}"
                    BorderBrush="{StaticResource BrushGutter}" BorderThickness="1"
                    CornerRadius="6" Padding="12,10">
              <ContentPresenter />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushBlue}" />
                <Setter TargetName="Bd" Property="Background" Value="#FF323842" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#FF3A4A5C" />
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrushCyan}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="OdOutlineItem" TargetType="ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0,0,0,4" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="10,8">
              <ContentPresenter />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#223E4451" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#443A4A5C" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ScrollBar">
      <Setter Property="Background" Value="{StaticResource BrushBgDeep}" />
      <Setter Property="Width" Value="10" />
    </Style>

    <Style TargetType="ScrollViewer">
      <Setter Property="Background" Value="Transparent" />
    </Style>
  </Application.Resources>
</Application>
